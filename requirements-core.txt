# 项目核心运行时依赖
# 这个文件包含项目运行所必需的最小依赖集合

# FastAPI核心依赖
fastapi==0.116.1
uvicorn==0.35.0
pydantic==2.11.7
pydantic-settings==2.10.1

# 数据库相关 (SQLite)
sqlalchemy==2.0.41
aiosqlite==0.21.0

# Redis缓存
redis==6.2.0

# 认证和安全
passlib==1.7.4
python-jose==3.5.0
python-multipart==0.0.20
cryptography==45.0.5
bcrypt==4.3.0

# 邮件验证
email-validator==2.2.0

# 文件处理
aiofiles==24.1.0

# 配置管理
python-dotenv==1.1.1

# 颜色输出
colorama==0.4.6

# 命令行工具
click==8.2.1

# 核心依赖（自动安装，无需手动指定但记录版本）
# annotated-types==0.7.0
# anyio==4.9.0
# cffi==1.17.1
# dnspython==2.7.0
# ecdsa==0.19.1
# greenlet==3.2.3
# h11==0.16.0
# idna==3.10
# pyasn1==0.6.1
# pycparser==2.22
# pydantic-core==2.33.2
# rsa==4.9.1
# six==1.17.0
# sniffio==1.3.1
# starlette==0.47.1
# typing-extensions==4.14.1
# typing-inspection==0.4.1

# 说明：
# - 这是项目的最小运行时依赖
# - 开发依赖请参考 requirements-dev.txt
# - 生产环境可能需要额外的依赖如 gunicorn
# - 可选功能的依赖已移至完整的 requirements.txt