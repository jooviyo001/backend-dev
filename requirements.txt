# FastAPI核心依赖
fastapi==0.116.1
uvicorn==0.35.0
pydantic==2.11.7
pydantic-settings==2.10.1

# 数据库相关
sqlalchemy==2.0.41
aiosqlite==0.21.0
# alembic==1.12.1  # 数据库迁移工具，可选
# psycopg2-binary==2.9.9  # PostgreSQL驱动，需要时取消注释

# Redis缓存
redis==6.2.0
# aioredis==2.0.1  # 可选的异步Redis客户端

# 认证和安全
passlib==1.7.4
python-jose==3.5.0
python-multipart==0.0.20
cryptography==45.0.5
bcrypt==4.3.0

# 邮件验证
email-validator==2.2.0

# 文件处理
aiofiles==24.1.0

# 配置管理
python-dotenv==1.1.1

# 颜色输出
colorama==0.4.6

# 命令行工具
click==8.2.1

# 核心依赖（自动安装）
annotated-types==0.7.0
anyio==4.9.0
cffi==1.17.1
dnspython==2.7.0
ecdsa==0.19.1
greenlet==3.2.3
h11==0.16.0
idna==3.10
pyasn1==0.6.1
pycparser==2.22
pydantic-core==2.33.2
rsa==4.9.1
six==1.17.0
sniffio==1.3.1
starlette==0.47.1
typing-extensions==4.14.1
typing-inspection==0.4.1

# 开发和测试依赖（可选，建议放在requirements-dev.txt中）
# pytest==7.4.3
# pytest-asyncio==0.21.1
# pytest-cov==4.1.0
# black==23.11.0
# isort==5.12.0
# flake8==6.1.0
# mypy==1.7.1

# 生产环境依赖（可选）
# gunicorn==21.2.0  # WSGI服务器
# uvloop  # 高性能事件循环（Linux/macOS）

# 注释掉的依赖说明：
# - aiolock==1.4.0  # 版本不存在
# - safety==2.3.5/3.0.1  # 与pydantic 2.x存在依赖冲突
# - psycopg2  # 需要PostgreSQL开发环境，已改用aiosqlite